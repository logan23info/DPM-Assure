"use client";
import {useState} from "react"; import {useRouter} from "next/navigation";
export function DeleteEngagement({organizationId,engagementId,name,status}:{organizationId:string;engagementId:string;name:string;status:string}){const[r,setR]=useState(false),[c,setC]=useState(""),[m,setM]=useState("");const router=useRouter();async function del(){if(c!==name)return setM("Type the engagement name exactly to confirm.");setM("Deleting…");const x=await fetch(`/api/organizations/${organizationId}/engagements/${engagementId}/delete`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({confirmation:c})});const b=await x.json();if(!x.ok)return setM(b.message??"Deletion failed");router.push(`/organizations/${organizationId}/engagements`);}if(status!=="PLANNING")return null;return <section className="workspace-panel"><h2>Delete engagement</h2>{!r?<><p>This permanently deletes the engagement, related database records, and private evidence objects.</p><button onClick={()=>setR(true)}>Delete permanently</button></>:<div className="auth-form"><p><strong>Warning:</strong> This cannot be undone. Type <code>{name}</code> to confirm.</p><input value={c} onChange={e=>setC(e.target.value)} placeholder="Engagement name"/><button onClick={()=>void del()}>Confirm permanent deletion</button><button type="button" onClick={()=>setR(false)}>Cancel</button></div>}{m&&<p className="form-message">{m}</p>}</section>}
