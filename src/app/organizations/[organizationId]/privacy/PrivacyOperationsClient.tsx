"use client";
import { FormEvent,useCallback,useEffect,useState } from "react";
type Item={id:string;name?:string;title?:string;state?:string;status?:string;decision?:string;purpose?:string;destinationCountry?:string;requestType?:string};type Data={clients:{id:string;name:string}[];activities:Item[];dpias:Item[];processors:Item[];transfers:Item[];dsrs:Item[];breaches:Item[]};
const list=(v:FormDataEntryValue|null)=>typeof v==="string"?v.split(",").map(x=>x.trim()).filter(Boolean):[];
export function PrivacyOperationsClient({organizationId}:{organizationId:string}){const[data,setData]=useState<Data|null>(null);const[message,setMessage]=useState("Loading privacy operations…");const load=useCallback(async()=>{const r=await fetch(`/api/organizations/${organizationId}/privacy`,{cache:"no-store"});if(!r.ok)throw new Error("Privacy operations could not be loaded");setData(await r.json());},[organizationId]);useEffect(()=>{load().then(()=>setMessage("")).catch((e:Error)=>setMessage(e.message));},[load]);async function submit(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();setMessage("Saving governed record…");const f=new FormData(e.currentTarget),body:Record<string,unknown>=Object.fromEntries(f.entries());body.action=action;["dataSubjectCategories","personalDataCategories","recipients","dataCategories"].forEach(k=>{if(k in body)body[k]=list(f.get(k));});if(action==="create_dsr")body.receivedAt=new Date().toISOString();if(action==="create_breach"){body.detectedAt=new Date().toISOString();body.notificationRequired=f.get("notificationRequired")==="on";}const r=await fetch(`/api/organizations/${organizationId}/privacy`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),out=await r.json() as {message?:string};if(!r.ok){setMessage(out.message??"Could not save record");return;}e.currentTarget.reset();await load();setMessage("Record saved.");}const activities=data?.activities??[];return <div className="admin-stack"><p className="form-message">{message}</p><Panel title="ROPA / processing activities" items={activities} render={<form className="privacy-form" onSubmit={e=>submit(e,"create_activity")}><input name="name" required placeholder="Processing activity name"/><input name="purpose" required placeholder="Purpose"/><select name="controllerProcessorRole"><option>CONTROLLER</option><option>PROCESSOR</option><option>JOINT_CONTROLLER</option></select><select name="clientId"><option value="">No client linkage</option>{data?.clients.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><input name="lawfulBasis" placeholder="Lawful basis"/><input name="dataSubjectCategories" placeholder="Data subjects, comma separated"/><input name="personalDataCategories" placeholder="Personal data, comma separated"/><input name="recipients" placeholder="Recipients, comma separated"/><input name="retentionSummary" placeholder="Retention summary"/><input name="securityMeasuresSummary" placeholder="Security measures summary"/><button className="primary-button">Add processing activity</button></form>}/><Panel title="DPIA management" items={data?.dpias??[]} render={<form className="privacy-form" onSubmit={e=>submit(e,"create_dpia")}><select name="processingActivityId" required defaultValue=""><option value="" disabled>Select processing activity</option>{activities.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select name="decision"><option>REQUIRED</option><option>NOT_REQUIRED</option><option>IN_PROGRESS</option><option>REJECTED</option></select><input name="screeningRationale" required placeholder="Screening rationale"/><input name="riskSummary" placeholder="Risk summary"/><input name="mitigationSummary" placeholder="Mitigation summary"/><input name="residualRisk" placeholder="Residual risk"/><button className="primary-button">Create DPIA</button></form>}/><Panel title="Processors and transfer assessments" items={[...(data?.processors??[]),...(data?.transfers??[])]} render={<><form className="privacy-form" onSubmit={e=>submit(e,"create_processor")}><input name="name" required placeholder="Processor name"/><input name="serviceDescription" required placeholder="Service description"/><input name="country" placeholder="Country"/><input name="contractReference" placeholder="Contract reference"/><input name="dpaReference" placeholder="DPA reference"/><input name="securityReviewStatus" placeholder="Security review status"/><button className="primary-button">Register processor</button></form><form className="privacy-form" onSubmit={e=>submit(e,"create_transfer")}><select name="processingActivityId" required defaultValue=""><option value="" disabled>Select processing activity</option>{activities.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><select name="processorId"><option value="">No processor linkage</option>{data?.processors.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><input name="destinationCountry" required placeholder="Destination country"/><select name="mechanism"><option>SCC</option><option>ADEQUACY</option><option>BCR</option><option>DEROGATION</option><option>OTHER</option></select><input name="mechanismReference" placeholder="Mechanism reference"/><input name="transferRiskAssessmentReference" placeholder="Transfer risk assessment ref"/><input name="supplementaryMeasures" placeholder="Supplementary measures"/><button className="primary-button">Record transfer assessment</button></form></>}/><Panel title="DSRs and breach register" items={[...(data?.dsrs??[]),...(data?.breaches??[])]} render={<><form className="privacy-form" onSubmit={e=>submit(e,"create_dsr")}><input name="requestType" required placeholder="DSR request type"/><input name="subjectReferenceHash" required placeholder="Lowercase SHA-256 subject reference"/><input name="dueAt" type="datetime-local"/><button className="primary-button">Record DSR</button></form><form className="privacy-form" onSubmit={e=>submit(e,"create_breach")}><input name="title" required placeholder="Breach title"/><input name="description" required placeholder="Factual description"/><input name="dataCategories" placeholder="Data categories, comma separated"/><input name="affectedSubjectsEstimate" type="number" min="0" placeholder="Affected subjects"/><input name="severity" placeholder="Severity"/><input name="notificationRationale" placeholder="Notification rationale"/><label><input name="notificationRequired" type="checkbox"/>Notification required</label><button className="primary-button">Record breach</button></form></>}/></div>}
function Panel({title,items,render}:{title:string;items:Item[];render:React.ReactNode}){return <section className="workspace-panel"><div className="section-heading"><h2>{title}</h2><span className="count-badge">{items.length}</span></div>{render}<div className="data-list">{items.length?items.map(x=><article className="data-row" key={x.id}><div><strong>{x.name??x.title??x.destinationCountry??x.requestType??"Privacy record"}</strong><span>{x.state??x.status??x.decision??"Recorded"}</span></div></article>):<div className="empty-state">No records yet.</div>}</div></section>}
