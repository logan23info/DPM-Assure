"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
type Option={id:string;label:string};
export function PrivacyOperationsClient({organizationId,activities,dpias,processors,transfers,dsrs,breaches}:{organizationId:string;activities:Option[];dpias:Option[];processors:Option[];transfers:Option[];dsrs:Option[];breaches:Option[]}){const router=useRouter();const[msg,setMsg]=useState<string|null>(null);const[busy,setBusy]=useState(false);async function send(action:string,payload:Record<string,unknown>){setBusy(true);setMsg(null);const r=await fetch(`/api/organizations/${organizationId}/privacy`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action,...payload})});const b=await r.json();setBusy(false);if(!r.ok){setMsg(b.message??"Privacy action rejected");return;}setMsg("Saved.");router.refresh();}
 return <section className="workspace-panel"><div className="section-heading"><div><p className="eyebrow">Privacy operations actions</p><h2>Governed operational workflows</h2></div></div>{msg&&<p className="form-message">{msg}</p>}<div className="organization-grid">
 <form className="organization-card auth-form" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);void send("create_activity",{name:d.get("name"),purpose:d.get("purpose"),controllerProcessorRole:d.get("role"),lawfulBasis:d.get("lawfulBasis")||undefined,dataSubjectCategories:String(d.get("subjects")??"").split(",").map(x=>x.trim()).filter(Boolean),personalDataCategories:String(d.get("data")??"").split(",").map(x=>x.trim()).filter(Boolean),specialCategoryData:d.get("special")==="on",retentionSummary:d.get("retention")||undefined,securityMeasuresSummary:d.get("security")||undefined});}}><h3>Create RoPA activity</h3><input name="name" required placeholder="Processing activity"/><textarea name="purpose" required placeholder="Purpose"/><select name="role" defaultValue="CONTROLLER"><option>CONTROLLER</option><option>PROCESSOR</option><option>JOINT_CONTROLLER</option></select><input name="lawfulBasis" placeholder="Lawful basis/reference"/><input name="subjects" placeholder="Data subject categories, comma-separated"/><input name="data" placeholder="Personal data categories, comma-separated"/><label><input name="special" type="checkbox"/> Special-category data</label><textarea name="retention" placeholder="Retention summary"/><textarea name="security" placeholder="Security measures summary"/><button disabled={busy}>Create activity</button></form>
 <form className="organization-card auth-form" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);void send("create_dpia",{processingActivityId:d.get("activityId"),screeningRationale:d.get("rationale"),decision:d.get("decision"),riskSummary:d.get("risk")||undefined,mitigationSummary:d.get("mitigation")||undefined,residualRisk:d.get("residual")||undefined});}}><h3>Create DPIA screening</h3><select name="activityId" required defaultValue=""><option value="" disabled>Processing activity</option>{activities.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select><select name="decision" defaultValue="REQUIRED"><option>REQUIRED</option><option>NOT_REQUIRED</option><option>IN_PROGRESS</option><option>REJECTED</option></select><textarea name="rationale" required placeholder="Screening rationale"/><textarea name="risk" placeholder="Risk summary"/><textarea name="mitigation" placeholder="Mitigation summary"/><textarea name="residual" placeholder="Residual risk"/><button disabled={busy}>Create DPIA</button></form>
 <form className="organization-card auth-form" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);void send(String(d.get("action")),{dpiaId:d.get("dpiaId")});}}><h3>DPIA workflow</h3><select name="dpiaId" required defaultValue=""><option value="" disabled>DPIA</option>{dpias.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select><select name="action" defaultValue="start_dpia"><option value="start_dpia">Start assessment</option><option value="approve_dpia">Approve assessment</option></select><button disabled={busy}>Apply DPIA transition</button></form>
 <form className="organization-card auth-form" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);void send("register_processor",{name:d.get("name"),serviceDescription:d.get("service"),country:d.get("country")||undefined,contractReference:d.get("contract")||undefined,dpaReference:d.get("dpa")||undefined,securityReviewStatus:d.get("securityReview")||undefined});}}><h3>Register processor</h3><input name="name" required placeholder="Processor name"/><textarea name="service" required placeholder="Service description"/><input name="country" placeholder="Country"/><input name="contract" placeholder="Contract reference"/><input name="dpa" placeholder="DPA reference"/><input name="securityReview" placeholder="Security review status"/><button disabled={busy}>Register processor</button></form>
 <form className="organization-card auth-form" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);void send("create_transfer",{processingActivityId:d.get("activityId"),processorId:d.get("processorId")||undefined,destinationCountry:d.get("country"),mechanism:d.get("mechanism"),mechanismReference:d.get("reference")||undefined,transferRiskAssessmentReference:d.get("tia")||undefined,supplementaryMeasures:d.get("measures")||undefined});}}><h3>Record international transfer</h3><select name="activityId" required defaultValue=""><option value="" disabled>Activity</option>{activities.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select><select name="processorId" defaultValue=""><option value="">No processor</option>{processors.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select><input name="country" required placeholder="Destination country"/><select name="mechanism" defaultValue="SCC"><option>ADEQUACY</option><option>SCC</option><option>BCR</option><option>DEROGATION</option><option>OTHER</option></select><input name="reference" placeholder="Mechanism reference"/><input name="tia" placeholder="Transfer risk assessment reference"/><textarea name="measures" placeholder="Supplementary measures"/><button disabled={busy}>Create transfer</button></form>
 <form className="organization-card auth-form" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);void send("create_dsr",{requestType:d.get("requestType"),subjectReferenceHash:d.get("subjectHash"),receivedAt:d.get("receivedAt"),dueAt:d.get("dueAt")||undefined});}}><h3>Register data-subject request</h3><input name="requestType" required placeholder="ACCESS / DELETE / CORRECT / etc."/><input name="subjectHash" required minLength={64} maxLength={64} placeholder="Lowercase SHA-256 subject reference"/><label>Received<input name="receivedAt" type="datetime-local" required/></label><label>Governed due date<input name="dueAt" type="datetime-local"/></label><button disabled={busy}>Create DSR</button></form>
 <form className="organization-card auth-form" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);void send("transition_dsr",{dsrId:d.get("dsrId"),status:d.get("status"),identityVerifiedAt:d.get("identityVerifiedAt")||undefined,outcome:d.get("outcome")||undefined});}}><h3>DSR workflow</h3><select name="dsrId" required defaultValue=""><option value="" disabled>DSR</option>{dsrs.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select><select name="status" defaultValue="IDENTITY_VERIFICATION"><option>IDENTITY_VERIFICATION</option><option>IN_PROGRESS</option><option>ON_HOLD</option><option>COMPLETED</option><option>REJECTED</option><option>CANCELLED</option></select><label>Identity verified at<input name="identityVerifiedAt" type="datetime-local"/></label><textarea name="outcome" placeholder="Outcome"/><button disabled={busy}>Transition DSR</button></form>
 <form className="organization-card auth-form" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);const notification=d.get("notificationRequired");void send("record_breach",{title:d.get("title"),detectedAt:d.get("detectedAt"),occurredAt:d.get("occurredAt")||undefined,description:d.get("description"),dataCategories:String(d.get("categories")??"").split(",").map(x=>x.trim()).filter(Boolean),affectedSubjectsEstimate:d.get("affected")?Number(d.get("affected")):undefined,severity:d.get("severity")||undefined,notificationRequired:notification==="UNDECIDED"?undefined:notification==="YES",notificationRationale:d.get("rationale")||undefined});}}><h3>Record privacy breach</h3><input name="title" required placeholder="Incident title"/><label>Detected<input name="detectedAt" type="datetime-local" required/></label><label>Occurred<input name="occurredAt" type="datetime-local"/></label><textarea name="description" required placeholder="Description"/><input name="categories" placeholder="Data categories, comma-separated"/><input name="affected" type="number" min="0" placeholder="Affected subjects estimate"/><input name="severity" placeholder="Severity"/><select name="notificationRequired" defaultValue="UNDECIDED"><option value="UNDECIDED">Notification undecided</option><option value="YES">Notification required</option><option value="NO">Notification not required</option></select><textarea name="rationale" placeholder="Notification decision rationale"/><button disabled={busy}>Record breach</button></form>
 <form className="organization-card auth-form" onSubmit={e=>{e.preventDefault();const d=new FormData(e.currentTarget);void send("transition_breach",{breachId:d.get("breachId"),status:d.get("status"),containmentSummary:d.get("containment")||undefined,notificationRationale:d.get("rationale")||undefined,authorityNotifiedAt:d.get("authorityAt")||undefined,subjectsNotifiedAt:d.get("subjectsAt")||undefined});}}><h3>Breach workflow</h3><select name="breachId" required defaultValue=""><option value="" disabled>Breach</option>{breaches.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select><select name="status" defaultValue="TRIAGE"><option>TRIAGE</option><option>INVESTIGATING</option><option>CONTAINED</option><option>NOTIFICATION_ASSESSMENT</option><option>NOTIFIED</option><option>CLOSED</option></select><textarea name="containment" placeholder="Containment summary"/><textarea name="rationale" placeholder="Notification rationale"/><label>Authority notified<input name="authorityAt" type="datetime-local"/></label><label>Subjects notified<input name="subjectsAt" type="datetime-local"/></label><button disabled={busy}>Transition breach</button></form>
 </div></section>;
}
